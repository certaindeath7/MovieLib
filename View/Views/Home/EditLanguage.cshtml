
@{
    ViewBag.Title = "EditLanguage";
}

<h2>Edit Language Page</h2>
<h3>@ViewBag.LanguageMessage</h3>

<div class="container">
    <!-- inserting section-->
    <h3> Add Media</h3>
    <div class="container">
        <div class="row">
            <form action="../EditLanguage/CreateLanguage" method="post">
                <!-- Admin choose to create / update or delete media -->
                <div class="form-label-group" style="margin-top:10px; display:inline;">
                    <select id="criteria" name="criteria" required>
                        <option value="">Select Creaate / Update / Delete</option>
                        <option value="Create">Create</option>
                        <option value="Update">Update</option>
                        <option value="Delete">Delete</option>
                    </select>
                </div>

                <div class="form-label-group" style="margin-top:10px; display:inline;">
                    <label style="margin-right:25px;">Language Name</label> <input type="text" name="LanguageName" class="form-control" placeholder="Language Name" style="display:inline; margin-bottom:10px" required>
                </div>

                <div class="form-label-group" style="margin-top:10px;display:inline;">
                    <label style="margin-right:15px;">Language ID</label> <input name="LanguageID" type="number" class="form-control" placeholder="LanguageID" style="display:inline; margin-bottom:10px " required>

                </div><br />


                <div><button class="btnSubmit" type="submit" style="margin-top:100px; float:left;">Action</button></div>
            </form>
        </div>
    </div>

    <!-- print out the table-->
    <div class="table">
        <table class="table">
            <thead>
                <tr>
                    <th scope="col">LID</th>
                    <th scope="col">LanguageName</th>

                </tr>
            </thead>
            <tbody>

                @foreach (var media in ViewBag.allMedia)
                {

                    <tr>
                        <th scope="row">@media.LID</th>
                        <td>@media.LanguageName</td>

                    </tr>

                }

            </tbody>
        </table>
    </div>


</div>

